apiVersion: v1
kind: Service
metadata:
  name: {{ include "journiv.fullname" . }}
  labels:
    {{- include "journiv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "journiv.selectorLabels" . | nindent 4 }}
